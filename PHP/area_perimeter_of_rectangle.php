<?php

    # area and perimeter of a rectangle
    # area = length × width
    # perimeter = 2 × (length + width)

    $length = 5;
    $width = 8;

    $area = $length * $width;
    $perimeter = 2 * ($length + $width);

    echo "Area: $area\nPerimeter: $perimeter";


?>